policy-enforcer-config:
  paths:
    - path: /api/historico-venda/*
      methods:
        - method: GET
          scopes: ["view"]
        - method: POST
          scopes: ["create"]
    - path: /api/historico-venda/{id}
      methods:
        - method: GET
          scopes: ["view"]
        - method: PUT
          scopes: ["update"]
        - method: DELETE
          scopes: ["delete"]
